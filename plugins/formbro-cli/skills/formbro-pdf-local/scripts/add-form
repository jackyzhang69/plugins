#!/usr/bin/env bash
# Skill-local wrapper around scripts/add-form.sh
# Usage: add-form <form_id>
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../.." && pwd)"
exec "$ROOT/scripts/add-form.sh" "$@"
