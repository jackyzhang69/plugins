# AnyWeb official plugin

AnyWeb's first public plugin exposes a native macOS Apple Silicon and Windows
x64 CLI for shared Portal identity, source-free asynchronous repair claim
lookup, Tell-Jacky feedback, and completion signals. It does not bundle private
site graphs, selectors, fixtures, client values, or a general website runner.
